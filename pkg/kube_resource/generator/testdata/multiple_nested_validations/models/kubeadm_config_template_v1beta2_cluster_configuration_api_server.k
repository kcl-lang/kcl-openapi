"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""


schema KubeadmConfigTemplateV1beta2ClusterConfigurationAPIServer:
    r"""
    apiServer contains extra settings for the API server control plane component

    Attributes
    ----------
    certSANs : [str], default is Undefined, optional
        certSANs sets extra Subject Alternative Names for the API Server signing cert.
    extraArgs : [KubeadmConfigTemplateV1beta2APIServerExtraArgs], default is Undefined, optional
        extraArgs is a list of args to pass to the control plane component.
        The arg name must match the command line flag name except without leading dash(es).
        Extra arguments will override existing default arguments set by kubeadm.
    extraEnvs : [KubeadmConfigTemplateV1beta2APIServerExtraEnvs], default is Undefined, optional
        extraEnvs is an extra set of environment variables to pass to the control plane component.
        Environment variables passed using ExtraEnvs will override any existing environment variables, or *_proxy environment variables that kubeadm adds by default.
        This option takes effect only on Kubernetes >=1.31.0.
    extraVolumes : [KubeadmConfigTemplateV1beta2APIServerExtraVolumes], default is Undefined, optional
        extraVolumes is an extra set of host volumes, mounted to the control plane component.
    """


    certSANs?: [str]

    extraArgs?: [KubeadmConfigTemplateV1beta2APIServerExtraArgs]

    extraEnvs?: [KubeadmConfigTemplateV1beta2APIServerExtraEnvs]

    extraVolumes?: [KubeadmConfigTemplateV1beta2APIServerExtraVolumes]


    check:
        len(certSANs) >= 1 if certSANs
        len(certSANs) <= 100 if certSANs
        all i in certSANs { len(i) <= 253 if i not in [None, Undefined] } if certSANs
        all i in certSANs { len(i) >= 1 if i not in [None, Undefined] } if certSANs
        len(extraArgs) >= 1 if extraArgs
        len(extraArgs) <= 100 if extraArgs
        len(extraEnvs) >= 1 if extraEnvs
        len(extraEnvs) <= 100 if extraEnvs
        len(extraVolumes) >= 1 if extraVolumes
        len(extraVolumes) <= 100 if extraVolumes


