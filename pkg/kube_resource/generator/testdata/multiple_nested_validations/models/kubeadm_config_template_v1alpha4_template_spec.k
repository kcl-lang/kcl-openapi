"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""


schema KubeadmConfigTemplateV1alpha4TemplateSpec:
    r"""
    spec is the desired state of KubeadmConfig.

    Attributes
    ----------
    files : [KubeadmConfigTemplateV1alpha4SpecFiles], default is Undefined, optional
        files specifies extra files to be passed to user_data upon creation.
    format : str, default is Undefined, optional
        format specifies the output format of the bootstrap data
    mounts : [[str]], default is Undefined, optional
        mounts specifies a list of mount points to be setup.
    postKubeadmCommands : [str], default is Undefined, optional
        postKubeadmCommands specifies extra commands to run after kubeadm runs
    preKubeadmCommands : [str], default is Undefined, optional
        preKubeadmCommands specifies extra commands to run before kubeadm runs
    useExperimentalRetryJoin : bool, default is Undefined, optional
        useExperimentalRetryJoin replaces a basic kubeadm command with a shell
        script with retries for joins.

        This is meant to be an experimental temporary workaround on some environments
        where joins fail due to timing (and other issues). The long term goal is to add retries to
        kubeadm proper and use that functionality.

        This will add about 40KB to userdata

        For more information, refer to https://github.com/kubernetes-sigs/cluster-api/pull/2763#discussion_r397306055.
    users : [KubeadmConfigTemplateV1alpha4SpecUsers], default is Undefined, optional
        users specifies extra users to add
    verbosity : int, default is Undefined, optional
        verbosity is the number for the kubeadm log level verbosity.
        It overrides the `--v` flag in kubeadm commands.
    clusterConfiguration : KubeadmConfigTemplateV1alpha4SpecClusterConfiguration, default is Undefined, optional
        cluster configuration
    diskSetup : KubeadmConfigTemplateV1alpha4SpecDiskSetup, default is Undefined, optional
        disk setup
    initConfiguration : KubeadmConfigTemplateV1alpha4SpecInitConfiguration, default is Undefined, optional
        init configuration
    joinConfiguration : KubeadmConfigTemplateV1alpha4SpecJoinConfiguration, default is Undefined, optional
        join configuration
    ntp : KubeadmConfigTemplateV1alpha4SpecNtp, default is Undefined, optional
        ntp
    """


    files?: [KubeadmConfigTemplateV1alpha4SpecFiles]

    format?: "cloud-config"

    mounts?: [[str]]

    postKubeadmCommands?: [str]

    preKubeadmCommands?: [str]

    useExperimentalRetryJoin?: bool

    users?: [KubeadmConfigTemplateV1alpha4SpecUsers]

    verbosity?: int

    clusterConfiguration?: KubeadmConfigTemplateV1alpha4SpecClusterConfiguration

    diskSetup?: KubeadmConfigTemplateV1alpha4SpecDiskSetup

    initConfiguration?: KubeadmConfigTemplateV1alpha4SpecInitConfiguration

    joinConfiguration?: KubeadmConfigTemplateV1alpha4SpecJoinConfiguration

    ntp?: KubeadmConfigTemplateV1alpha4SpecNtp


